NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-secure-secret
# For Docker Compose, use host `db`. For local dev, use `localhost`.
DATABASE_URL=postgresql://pathwise:pathwise@db:5432/pathwise

# Local email testing (Docker Compose adds a MailHog service)
EMAIL_SERVER_HOST=mailhog
EMAIL_SERVER_PORT=1025
EMAIL_SERVER_USER=
EMAIL_SERVER_PASSWORD=
EMAIL_FROM="Pathwise" <noreply@pathwise.ai>

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
